# DENGO POS — Frontend environment variables
# Copy to .env and fill in real values.

# Leave empty in development — Vite's dev proxy forwards /api to the backend
# (see vite.config.ts). Set to the backend's public URL only for a production
# build that is NOT served behind the same reverse proxy as the API.
VITE_API_URL=
